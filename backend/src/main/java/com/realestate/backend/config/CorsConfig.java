package com.realestate.backend.config;

/**
 * CORS is now handled by SecurityConfig.
 * This file is intentionally empty — kept to avoid compilation issues with any
 * imports.
 * The actual CORS configuration lives in
 * com.realestate.backend.security.SecurityConfig.
 */
